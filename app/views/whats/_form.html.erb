<%= form_with(model: what) do |form| %>
  <% if what.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(what.errors.count, "error") %> prohibited this what from being saved:</h2>

      <ul>
        <% what.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
