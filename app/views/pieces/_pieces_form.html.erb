<%= form_with(model: el) do |form| %>
  <% if el.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(who.errors.count, "error") %> prohibited this who from being saved:</h2>

      <ul>
        <% who.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :content, class:"form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "#{ action_name.capitalize } #{ el.model_name.human} ", class:"btn btn-fight auto-size" %>
    <%= link_to "<i class='fas fa-level-down-alt rotate90'></i>".html_safe, 'javascript:history.go(-1);', class: "btn btn-run back" %>
  </div>
<% end %>